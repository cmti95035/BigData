package com.cmti.analytics.app.tracking.hbase.domain;

import com.cmti.analytics.app.tracking.hbase.domain.bean.DriveTestBean;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class DriveTest extends DriveTestBean {
	protected static final Logger logger = LogManager.getLogger(DriveTest.class);

}
